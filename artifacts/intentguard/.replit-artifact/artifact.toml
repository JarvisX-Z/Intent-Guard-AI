kind = "web"
previewPath = "/"
title = "IntentGuard AI"
version = "1.0.0"
id = "artifacts/intentguard"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22087

[services.development]
run = "pnpm --filter @workspace/intentguard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/intentguard", "run", "build" ]
publicDir = "artifacts/intentguard/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22087"
BASE_PATH = "/"
